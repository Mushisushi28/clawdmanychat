{
  "name": "manychat-webhook",
  "version": "1.0.0",
  "description": "Webhook receiver for ManyChat integration",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}